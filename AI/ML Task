# -*- coding: utf-8 -*-
"""AI_Engineer_Task.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1MCLE_9wBWgGzUdNtEyC9mjnhzrwjf8lJ
"""

import numpy as np
import pandas as pd
from sklearn.metrics import accuracy_score
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier


# 1) Load dataset
df = pd.read_csv("Titanic-Dataset.csv")
X = df.drop("Survived", axis=1)
y = df["Survived"]


print("Dataset chosen: Titanic dataset") # Corrected dataset name
print("Total rows:", len(X))
print("Missing values before preprocessing:")
print(X.isna().sum())

# 2) Basic preprocessing (handle missing values)
X = X.drop(['Name', 'Ticket', 'Cabin'], axis=1)
X['Sex'] = X['Sex'].replace({'male': 0, 'female': 1})
X['Age'].fillna(X['Age'].mean(), inplace=True)
most_frequent_embarked = X['Embarked'].mode()[0]
X['Embarked'].fillna(most_frequent_embarked, inplace=True)
X = pd.get_dummies(X, columns=['Embarked'], drop_first=True)
X.dropna(inplace=True)

print("\nMissing values after preprocessing:")
print(X.isna().sum())


# 3) Split into train/test
X_train, X_test, y_train, y_test = train_test_split(
    X, # Now using the preprocessed X
    y,
    test_size=0.2,
    random_state=42,
    stratify=y,
)

# 4) Train model
model = DecisionTreeClassifier(random_state=42)
model.fit(X_train, y_train)

# 5) Evaluation metric
y_pred = model.predict(X_test)
acc = accuracy_score(y_test, y_pred)
print("\nModel used: DecisionTreeClassifier")
print(f"Accuracy: {acc:.4f}")

# 6) Show 2-3 sample predictions
sample_count = 2
sample_X = X_test.head(sample_count)
sample_true = y_test.head(sample_count)
sample_pred = model.predict(sample_X)
